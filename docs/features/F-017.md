# F-017: Save Format Hardening

## Summary

Replace raw same-version memory snapshots with a versioned, portable save format suitable for real users.

## Reference

- Docs: `docs/features/F-011.md`, `BOUNDARIES.md`
- Code: `Sources/AkalabethMac/AkalabethPersistence.swift`, `Sources/AkalabethMac/GameSession.swift`

## Scope

- Define a versioned save schema for game state and adapter metadata.
- Reject or migrate incompatible saves safely.
- Add metadata such as timestamp, app version, game mode, and display name.
- Keep filesystem behavior in the Mac persistence adapter.

## Acceptance Criteria

- Save files are not raw `AkGameState` memory dumps.
- Corrupt and unsupported-version saves fail gracefully.
- Round-trip tests cover representative modes.
- Save/resume remains documented as a modern addition, not original behavior.

## Validation

```bash
swift test
make -C harness test
```

## Status

Implemented.

## Implementation Notes

- Saves are now UTF-8 JSON documents at `~/Library/Application Support/Akalabeth/SaveGame.json`, not raw `AkGameState` memory snapshots.
- The save document includes a format identifier, schema version, timestamp, app version, game mode, display name, portable core fields, and adapter prompt/input state.
- Resume rejects corrupt JSON, unknown format identifiers, unsupported schema versions, invalid C enum raw values, and malformed fixed-array lengths by returning no resumable session.
- Save/resume remains a modern Mac convenience; the original Applesoft distribution remains documented as having no historical save-game option.

Validation last run:

```bash
swift test
make -C harness test
rg -n "AppKit|SwiftUI|Metal|AVFoundation|Foundation|SDL|NS[A-Z][A-Za-z]+\\b|NSURL|Sandbox" Core
```
